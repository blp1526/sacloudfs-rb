require 'spec_helper'

describe Sacloudfs::Rb do
  it 'has a version number' do
    expect(Sacloudfs::Rb::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
